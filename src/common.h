#ifndef COMMON_H
#define COMMON_H

#include <stddef.h>
#include <stdbool.h>

#include "basearm.h"
#include "stddefs.h"
#include "str712.h"

#define ARRAY_SIZE(a) (sizeof(a)/sizeof(a[0]))

// common debugging options
#define DEBUG_SERIAL

#endif // COMMON_H
